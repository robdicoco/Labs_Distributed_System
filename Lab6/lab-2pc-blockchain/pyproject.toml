[project]
name = "lab-2pc-blockchain"
version = "0.1.0"
description = "2PC coordinator and bank participants + Sepolia CommitLog"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
]
